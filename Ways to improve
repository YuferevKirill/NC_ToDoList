Пути совершенствования:
done 1. Переделать отдельные сущности в отдельные классы.
done 2. Переделать реализацию попапа. Конструктор должен принимать модель полей с метаинформацией.
done 3. Сделать сохранение и загрузку из Backendless.
done 4. Подумать над разными путями реализации обновления View - обновлять весь View, отдельными кусочками, точечно.
5. При загрузке описания задачи реализовать промис.

done 6. Реализовать попап на основе модели. Архитектура: AbstractPopup -> CreationPopup
done 7. Реализовать вспомогательный класс, который вернёт форму.
8. Реализовать редактирование задачи с помощью попапа.
half done (отдельный файл) 9. Все действия с хранилищем должны быть в одном классе.
10. Разделить код на несколько файлов.

11. закрытие попапа
12. удаление задачи

{
	items: [
		{type: input, name: "", callback:..}
	]
}
